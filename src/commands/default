import { Collection, CommandInteraction } from "discord.js";
import chalk from "chalk";

import { DaClient } from "../../resources/definitions.js";
import { botLog } from "../../resources/automaton.js";

const data = {
    name: "ping",
    description: "sender ping",
    options: [
        {
            name: "full",
            type: "BOOLEAN",
            description: "mye ræl?"
        }
    ]
}

export default data;
export async function run(client: DaClient, interaction: CommandInteraction, args: Collection<string, any>) {
    const { fGreen } = client.formattedColours;

    botLog(chalk `{${fGreen} COMMAND} {grey > USED COMMAND}`);
};